<?xml version="1.0" encoding="utf-8"?>
<!-- Manifest fuso con quello di v1 (origina_res/AndroidManifest.xml):
     icona launcher originale, tema XML, permessi di rete/notifiche (per il
     download del modello, Fase 4) e regole di backup. NON portati: le
     Activity multiple (Ex è single-activity), i service stdf (feature
     morta) e network_security_config (era per il backend locale stdf).
     Il service WorkManager foreground si aggiungerà col DownloadWorker
     in Fase 4. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Servono dalla Fase 4 (download modello Gemma con notifica). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImmundaNoctis">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ImmundaNoctis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Accelerazione GPU del motore di inferenza (LiteRT-LM/OpenCL).
             Opzionali: se mancano si ripiega su CPU. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libvndksupport.so" android:required="false" />

        <!-- Il download del modello (GB) prosegue con l'app in background,
             come servizio in foreground con notifica di progresso. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>
